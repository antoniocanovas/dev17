<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="ba_update_packing_prices_from_base_product" model="base.automation">
        <field name="name">=> Actualiza precios de PACKING al cambiar el de producto base</field>
        <field name="model_id" ref="product.model_product_pricelist_item"/>
        <field name="trigger">on_create_or_write</field>
        <field name="active" eval="True"/>
    </record>

    <record id="as_update_packing_prices_from_base_product" model="ir.actions.server">
        <field name="name">=> AS Actualiza precios de PACKING al cambiar el de producto base</field>
        <field name="model_id" ref="product.model_product_pricelist_item"/>
        <field name="evaluation_type">value</field>
        <field name="state">code</field>
        <field name="code">
            record.update_packing_products_price()
        </field>
        <field name="base_automation_id" ref="sale_inplast.ba_update_packing_prices_from_base_product"/>
    </record>

    <record id="action_pricelist_recalculation" model="ir.actions.server">
        <field name="name">=> Recalcular precios</field>
        <field name="model_id" ref="product.model_product_pricelist"/>
        <field name="binding_model_id" ref="product.model_product_pricelist"/>
        <field name="state">code</field>
        <field name="code">
for pricelist in records:
    pricelist.products_pricelist_recalculation()
        </field>
    </record>
    <record id="action_pricelist_price_recalculation" model="ir.actions.server">
        <field name="name">=> Actualizar precios recalculados</field>
        <field name="model_id" ref="product.model_product_pricelist"/>
        <field name="binding_model_id" ref="product.model_product_pricelist"/>
        <field name="state">code</field>
        <field name="code">
for pricelist in records:
    pricelist.pricelist_update_confirm()
        </field>
    </record>

</odoo>
