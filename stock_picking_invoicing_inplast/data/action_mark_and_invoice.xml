<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_mark_and_invoice_server" model="ir.actions.server">
        <field name="name">Marcar para facturar y crear facturas borrador</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="binding_model_id" ref="stock.model_stock_picking"/>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">action = records.action_mark_and_invoice()</field>
    </record>
</odoo>
