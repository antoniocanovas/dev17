<?xml version='1.0' encoding='UTF-8'?>
<odoo>

    <record id="invoice_risk_contract_inh_form" model="ir.ui.view">
        <field name="name">invoice.risk.contract.inh.form</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <field name="invoice_date" position="after">
                <field name="risk_batch_id"
                       readonly="1"
                       invisible="(risk_batch_id == False)"/>
            </field>
        </field>
    </record>

</odoo>